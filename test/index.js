var Lab = require("lab"),
    lab = exports.lab = Lab.script(),
    describe = lab.experiment,
    before = lab.before,
    it = lab.test,
    assert = require("assert")

describe("API", function () {
  it("should start a server which returns a 200 on /", function (done) {
    assert.ok(false, "not implemented")
    done()
  })
})
